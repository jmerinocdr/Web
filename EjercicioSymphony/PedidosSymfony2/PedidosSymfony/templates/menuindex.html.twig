<div class="menu">
    <a href="{{ path ( 'default' ) }}">Volver al inicio</a>
    {% if (controller_name == "Register") %}
        <a href="{{ path ( 'app_login' ) }}">Ir al login</a>
    {% endif %}
    {% if (controller_name == "Login") %}
        <a href="{{ path ( 'app_register' ) }}">Ir al registro</a>
    {% endif %}
    
</div>